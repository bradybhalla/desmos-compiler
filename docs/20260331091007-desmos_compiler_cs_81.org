# Created 2026-05-03 Sun 22:44
#+title: Desmos compiler (CS 81)
#+author: Brady Bhalla
#+filetags: :course:

Took Spring 2026.

Recreating my [[id:C5CA94BD-0E15-4906-9F17-9F3F72586A5B][Desmos compiler]] in a more principled way.
* Meeting notes
** Meeting 3/31
Next up:
- Nail down passes and initial language features
- Set up codebase
- Start implementing the backend
** Meeting 4/7
- Set up codebase
- "Register_func_instrs" language
- "Register_stack_instrs" language
- Partial "convert_functions_to_stack" pass from Register_func_instrs -> Register_stack_instrs
** Meeting 4/14
*** DONE Document intermediate languages
docs/ folder has two files (just directly published from my org-roam notes)
*** DONE Mostly finished basic implementation of backend
Still need to make the actual desmos output pass, but it should be pretty simple
*** DONE VM interpreter
*** DONE Testing
The testing is very basic but it should give a good enough base for now. At some
point I should probably write a few larger programs that I can test the
input/output of as they go through each pass.
** Meeting 4/21
*** DONE finish generate output pass
The language type matches the Desmos expression format. The program can be printed as
javascript and pasted into the console which will populate the Desmos graph with
everything needed to run the program.
*** DONE Languages Refactor
I wanted to make the languages slightly different so hopefully future
optimizations will be much easier. I now keep instructions in blocks by label,
handle conditionals in a way that integrates into Desmos more easily, and handle
control flow slightly differently. I also separated out some logic into separate
passes (namely for computing live registers and extracting register info). These
two passes are still very basic but at least they are their own passes so can be
made more complex in the future.

- [X] put general information like what registers are used in any given block of
  code in a much earlier pass. Right now I do a lot of repeated work.
- [X] liveness analysis into a new pass (still keep very basic for now). Also
  can compress blocks with only one incoming block.
- [X] Put instructions in blocks instead of labels alongside instructions
- [X] maybe keep the idea of jump with link and return even in the last
  language. I think this would help with compression optimizations later. or
  maybe the compression should happen in the stack language instead? probably
  not because this wouldn't allow for compressing the PC.
*** DONE Better testing
- [X] smaller targeted tests
- [X] once frontend is ready do a medium/large program that will hopefully catch
  small issues (not ready yet but it can be very quickly added)
** Meeting 4/28
*** DONE Design frontend language
"C_style_frontend"
*** DONE Pass to extract function calls from expressions
"Extract function calls and defs"
** TODO [5/8] Meeting 5/5
*** DONE Explicate control pass
*** DONE Tests for passes from last week
See "test_program_correctness"
*** DONE split if statements pass
- if, and, or, if_expr all run in 1 step if there are no side effects
- if there are side effects the short circuiting gets handled correctly
*** DONE error checking pass for functions
- function defs in the correct place
- no duplicate functions or params
- all branches of a function return
- kind of annoying to put everything through Or_error monad. it is probably
  better to have separate checking passes that use the monad and regular compiler
  passes can assume everything is safe
- encode error checking state into the type system
*** DONE tests for function error checking pass
- most extensive tests I have done so far
- much easier since they can be written in the frontend language
*** TODO Variable scope
*** TODO Types
*** TODO Error checking and error checking pass
*** I will be out of town next week!!
** Future plans
*** Lists (and lists of lists)
- could be stored like
  [ 1st start ind, 2nd start ind, ...,
1st->   length, val1, val2, val3
2nd->   length, val1, val2
   ]
- with a lot of very careful indexing we could have arbitrary lists
- now how do we handle the length cap?
*** Desmos expression integration
- Desmos expressions the user can define.
- All variables used in them must be global and are not allowed to be put on a
  stack, so some renaming is needed
*** Compression
- compress instructions within a block. graph based approach? I think there
  should be a greedy way to do it optimally?
